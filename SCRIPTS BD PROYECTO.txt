CREATE IMAGENES CAPAS
(
	CI INT NOT NULL IDENTITY(1,1) PRIMARY KEY,
    IMAGEN IMAGE,
    NOMBRE VARCHAR(25)
)


CREATE TABLE CAPAS
(
	ID INT NOT NULL IDENTITY(1,1) PRIMARY KEY,
    IMAGEN IMAGE,
    NOMBRE VARCHAR(25)
)